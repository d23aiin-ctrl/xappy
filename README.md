# Xappy

This repository contains the Xappy workspace.

## Structure

- `uk_oil/` — main application code and docs
- `xapenv/` — local Python environment (excluded from git)

## Notes

- Build artifacts and virtual environments are excluded via `.gitignore`.
